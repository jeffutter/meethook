name: CI # Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

jobs:
  test:
    name: Test Suite (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v22
      - uses: DeterminateSystems/magic-nix-cache-action@v14
        timeout-minutes: 15
        with:
          use-flakehub: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test-${{ matrix.os }}
          workspaces: ". -> target"
      - name: Run tests
        run: nix develop -c cargo nextest run --all-features --workspace
      - name: Run doctests
        run: nix develop -c cargo test --doc --all-features --workspace

  # Linux-only, unlike the other three matrix jobs: rustfmt formats source text without
  # evaluating `#[cfg(...)]`, so it already checks every line of macOS-gated code even when
  # run on Linux. Clippy and Docs stay cross-platform because they type-check per target and
  # would otherwise miss the `cfg(target_os = "macos")` branches in meethook and
  # meethook-transcribe.
  rustfmt:
    name: Rustfmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v22
      - uses: DeterminateSystems/magic-nix-cache-action@v14
        timeout-minutes: 15
        with:
          use-flakehub: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rustfmt-ubuntu-latest
          workspaces: ". -> target"
      - name: Check formatting
        run: nix develop -c cargo fmt --all --check

  clippy:
    name: Clippy (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v22
      - uses: DeterminateSystems/magic-nix-cache-action@v14
        timeout-minutes: 15
        with:
          use-flakehub: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: clippy-${{ matrix.os }}
          workspaces: ". -> target"
      - name: Clippy check
        run: nix develop -c cargo clippy --all-targets --all-features --workspace -- -D warnings

  docs:
    name: Docs (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v22
      - uses: DeterminateSystems/magic-nix-cache-action@v14
        timeout-minutes: 15
        with:
          use-flakehub: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: docs-${{ matrix.os }}
          workspaces: ". -> target"
      - name: Check documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: nix develop -c cargo doc --no-deps --document-private-items --all-features --workspace

  # meethook-record roots its own workspace (see crates/meethook-record/Cargo.toml) because
  # its Apple-framework dependencies cannot compile off macOS, so `--workspace` above never
  # touches it -- mirrored from lefthook.yml's pre-push `record-crate` gate, which runs the
  # same three commands from inside the same directory on Darwin only.
  record:
    name: meethook-record (macOS-only workspace)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@v22
      - uses: DeterminateSystems/magic-nix-cache-action@v14
        timeout-minutes: 15
        with:
          use-flakehub: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: record
          workspaces: "crates/meethook-record -> target"
      - name: fmt, clippy, test
        run: |
          nix develop -c bash -c '
            set -euo pipefail
            cd crates/meethook-record
            cargo fmt --all --check
            cargo clippy --all-targets --workspace -- -D warnings
            cargo nextest run --workspace
          '
